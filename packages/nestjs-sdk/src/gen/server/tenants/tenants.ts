/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Fivenines Feature Flags Control Plane
 * Management API for multi-tenant feature flags and remote config.
 *
 * **Success shapes**
 * - Paginated lists: `{ list: T[], pageInfo: PageInfo }`
 * - Single resources: resource schema at the root (no envelope)
 *
 * **Errors** (4xx/5xx): `{ message?: string, fields?: { field, message }[] }`
 * OpenAPI spec version: 1.0.0
 */
import type {
  TenantListResponseDto,
  TenantsControllerListTenantsParams
} from '../../model';

import { customFetch } from '../../../mutator.server';

export const getTenantsControllerListTenantsUrl = (params?: TenantsControllerListTenantsParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v1/tenants?${stringifiedParams}` : `/api/v1/tenants`
}

/**
 * @summary List tenants visible to the current tenant context
 */
export const tenantsControllerListTenants = async (params?: TenantsControllerListTenantsParams, options?: Parameters<typeof customFetch>[1]): Promise<TenantListResponseDto> => {

  return customFetch<TenantListResponseDto>(getTenantsControllerListTenantsUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


